<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyShot"
        tools:targetApi="31">
        <activity
            android:name="loginProcess.OTPActivity"
            android:exported="false" />
        <activity
            android:name="loginProcess.UserInfoActivity"
            android:exported="false" />
        <activity
            android:name="loginProcess.LoginActivity"
            android:exported="false" />
        <activity
            android:name="loginProcess.GetStartActivity"
            android:exported="false" />
        <activity
            android:name="loginProcess.SplashScreen"
            android:exported="true"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>